# Program to calculate the area of a triangle

baseLength = float(input("Enter the base of the triangle (in units): "))
heightLength = float(input("Enter the height of the triangle (in units): "))

# Area formula: 0.5 * base * height
area = 0.5 * baseLength * heightLength

print("The area of the triangle is:", area, "square units")
